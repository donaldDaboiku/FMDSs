# Used by docker compose (repo root). Do not commit real secrets.
JWT_SECRET=replace-with-a-long-random-secret
CLIENT_ORIGIN=http://localhost:5000
PORT=5000

# For Atlas / cloud (used by docker-compose.atlas.yml and Render):
# MONGODB_URI=mongodb+srv://USER:PASS@cluster.mongodb.net/fmds
